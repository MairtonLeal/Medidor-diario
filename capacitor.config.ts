import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.medidordiario',
  appName: 'Medidor Diário',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
